package com.bit.loop;

public class ForLoop {
   public static void main(String[] args) {
	   String opcode = "";
	   int oddsum = 0, even = 0; 
	   for(int i = 0; i <= 100; i++)
		   if( i % 2 == 0) {
			   if( i == 100) {
				   opcode = i + " = "; 
			   }else {
				   opcode = i + " + ";
			   }
			
			   
		   }else {
			   
		   }
   }
}
